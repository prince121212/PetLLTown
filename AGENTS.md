# 项目协作约定

## Git 提交习惯

- 默认使用中文编写 commit message，除非用户明确要求使用其他语言。
- commit message 要简洁描述本次改动目的，优先说明用户可感知的结果。

## 视频素材验收习惯

- 后续进入项目的主宠物透明视频素材，默认按 `素材交付规范.md` 验收。
- 接入素材前先确认源文件真的带 alpha 通道，不把普通黑底、白底、绿幕或 `yuv420p` 普通视频当透明素材使用。
- 小程序运行时素材统一走双通道 H.264 MP4，不恢复 PNG 帧序列兜底，除非用户明确要求。
